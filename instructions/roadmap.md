- add a RAG to access information about the company just in case it's local calculus

- in order to do the KPI probability, I'd create synthetic data and use a model in order to do those predictions
maybe using an LLM as a model (ideally)

- for public models (groq) -> don't send the steps, just talk about a process that has X data. Then their result send it to the local one with the "sensitive" data and get results
